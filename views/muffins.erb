class Muffins 
    attr_accessor :price, :description, :image
    def initialize (price, description, image)
        @price = price
        @description = description
        @image = image
    end
end
        